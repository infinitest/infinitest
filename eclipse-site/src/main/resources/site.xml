<site>
    <description url="${plugin.update.url}">Infinitest Update Site</description>
    <feature url="features/${feature.id}_${plugin.version}.jar" id="${feature.id}"
             version="${plugin.version}">
        <category name="Infinitest" />
    </feature>
    <category-def name="Infinitest" label="Infinitest for Eclipse" />
</site>